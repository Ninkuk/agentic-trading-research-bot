__pycache__/
*.pyc
.mypy_cache/
.ruff_cache/
.pytest_cache/
.venv/
screener.db
*.db-wal
*.db-shm
.env
.playwright-mcp/
scratchpad/

# Claude Code: the project skills ship (account-positions and journal-sync are the
# only paths live account state enters the system), personal config does not.
.claude/*
!.claude/skills/
data/
.DS_Store
logs/
reports/
docs/plans/
docs/superpowers/

# dashboard/ (React app) — build output and the dev-server copy of the
# fixture are derived, never checked in.
dashboard/node_modules/
dashboard/dist/
dashboard/public/data.json

# Local worktrees for isolated task execution (never committed).
.worktrees/
